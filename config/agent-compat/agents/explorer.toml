# Explorer — traces execution paths and maps architecture.
# Maps to the "Exploration subagents" tier in AGENTS.md.

name = "explorer"
description = "Traces execution paths, maps component dependencies, and returns evidence. Never proposes fixes unless explicitly asked."
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
