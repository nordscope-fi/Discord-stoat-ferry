# Locator — fast mechanical file and symbol search.
# Maps to the "File-location sweeps" tier in AGENTS.md.

name = "locator"
description = "Fast file and symbol search for Ferry."
model = "gpt-5.6-luna"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
developer_instructions = """
Find files by pattern, grep for symbols and keywords, locate definitions and references. Return paths and line numbers. No analysis, no design.
"""
